#include "CommonFunctions.h"

cv::Mat convertHSV(const cv::Mat& image)
{
	cv::Mat resultImage = cv::Mat(image.size(), CV_8UC3);
	for(int x = 0; x < image.cols; ++x)
		for(int y = 0; y < image.rows; ++y)
		{
		}
	return resultImage;
}
